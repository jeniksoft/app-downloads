# Kontrolní Součty

Soubor `SHA256SUMS.txt` obsahuje SHA-256 kontrolní součty veřejných setupů v tomto repu.

Ve Windows můžeš soubor ověřit například takto:

```powershell
Get-FileHash -Algorithm SHA256 .\WindowsUpdateRestartGuardSetup.exe
```
